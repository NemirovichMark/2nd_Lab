// Лабораторная работа № 2
// Уровень 1
// Номер 3
/*#region
double c;
double a = Convert.ToDouble(Console.ReadLine());
double b = Convert.ToDouble(Console.ReadLine());
if(a > 0)
{
    c = Math.Min(a, b);
}
else
{
    c = Math.Max(a, b);
}
Console.WriteLine(c);
#endregion*/

// Номер 4
/*#region
double z;
double a = Convert.ToDouble(Console.ReadLine());
double b = Convert.ToDouble(Console.ReadLine());
double c = Convert.ToDouble(Console.ReadLine());
z = Math.Max(Math.Min(a, b), c);
Console.WriteLine(z);
#endregion*/

// Уровень 2
// Номер 1
/*#region
using System.Diagnostics.CodeAnalysis;

int n = 5;
int i = 1;
double sum = 0;
while (i <= n)
{
    Console.WriteLine("Введите рост в метрах: ");
    double height = Convert.ToDouble(Console.ReadLine());
    sum += height;
    i++;
}
Console.WriteLine(sum);

#endregion*/

// Номер 9
/*#region
int n = 5;
double cur, prev = 10000000000000000, min = 0;
for (int i = 0; i < n; i++)
{
    Console.WriteLine("Введите результат в секундах: ");
    cur = Convert.ToDouble(Console.ReadLine());
    min = Math.Min(cur, prev);
    prev = cur;
}
Console.WriteLine(min);
#endregion*/

// Уровень 3
// Номер 4
/*#region
int n, i = 0;
Console.WriteLine("Введите первый радиус");
double r1 = Convert.ToDouble(Console.ReadLine());
Console.WriteLine("Введите второй радиус");
double r2 = Convert.ToDouble(Console.ReadLine());
Console.WriteLine("Введите количество точек, которые хотите проверить");
n = Convert.ToInt32(Console.ReadLine());
while (n > 0)
{
    Console.WriteLine("Введите координату Х: ");
    double x = Convert.ToDouble(Console.ReadLine());
    Console.WriteLine("Введите координату Y: ");
    double y = Convert.ToDouble(Console.ReadLine());
    if (x * x + y * y > r1 * r1 && x * x + y * y <= r2 * r2)
    {
        i++;
    }
    n--;
}
Console.WriteLine(i);
#endregion*/

// Номер 11
/*#region
using System.Globalization;
using System.Runtime.Versioning;

Console.WriteLine("Введите количество студентов:");
int n = Convert.ToInt32(Console.ReadLine());
int m1, m2, m3, m4; //Оценки
double count = n * 4; //Количество оценок
double sum = 0; //Сумма оценок(всех)
double cur = 0, prev = 0, min = 100000000000000;
while (n > 0)
{
    for (int i = 1; i <= n; i++)
    {
        Console.WriteLine("Введите оценки студента:");
        m1 = Convert.ToInt32(Console.ReadLine());
        m2 = Convert.ToInt32(Console.ReadLine());
        m3 = Convert.ToInt32(Console.ReadLine());
        m4 = Convert.ToInt32(Console.ReadLine());
        sum += m1 + m2 + m3 + m4;
        cur = (m1 + m2 + m3 + m4) / 4;
        min = Math.Min(min, cur);
        prev = cur;
        n--;
    }
}
Console.WriteLine("Средний балл группы: " + sum / count);
#endregion*/

// Номер 12
/*#region
Console.WriteLine("Введите количество повторов:");
int n = Convert.ToInt32(Console.ReadLine());
while (n > 0)
{
    Console.WriteLine("Что хотите найти: 1 = площадь квадрата," + " 2 = площадь круга," + " 3 = площадь равностороннего треугольника");
    Console.WriteLine("Введите выбор:");
    int k = Convert.ToInt32(Console.ReadLine());
    switch (k)
    {
        case 1:
            Console.WriteLine("Введите длину стороны:");
            double r = Convert.ToDouble(Console.ReadLine());
            Console.WriteLine(r * r);
            break;
        case 2:
            Console.WriteLine("Введите радиус окружности:");
            r = Convert.ToDouble(Console.ReadLine());
            Console.WriteLine(Math.PI * r * r);
            break;
        case 3:
            Console.WriteLine("Введите длину стороны:");
            r = Convert.ToDouble(Console.ReadLine());
            Console.WriteLine(r * r * Math.Pow(3, 0.5) / 4);
            break;
        default:
            Console.WriteLine("Неверный выбор. Выберите 1, 2 или 3!");
            break;
    }
    n--;
}
#endregion*/

// Номер 13
/*#region
Console.WriteLine("Введите количество повторов:");
int n = Convert.ToInt32(Console.ReadLine());
while (n > 0)
{
    Console.WriteLine("Что хотите найти: 1 = площадь прямоугольника," + " 2 = площадь кольца," + " 3 = площадь равнобедренного треугольника");
    Console.WriteLine("Введите выбор:");
    int k = Convert.ToInt32(Console.ReadLine());
    switch (k)
    {
        case 1:
            Console.WriteLine("Введите длины сторон:");
            double A = Convert.ToDouble(Console.ReadLine());
            double B = Convert.ToDouble(Console.ReadLine());
            Console.WriteLine(A * B);
            break;
        case 2:
            Console.WriteLine("Введите радиусы окружностей:");
            A = Convert.ToDouble(Console.ReadLine());
            B = Convert.ToDouble(Console.ReadLine());
            Console.WriteLine(Math.Abs(Math.PI * A * A - Math.PI * B * B));
            break;
        case 3:
            Console.WriteLine("Введите длины сторон, где первое введенное число основание:");
            A = Convert.ToDouble(Console.ReadLine());
            B = Convert.ToDouble(Console.ReadLine());
            Console.WriteLine(A * Math.Pow((4 * B * B - A * A), 0.5) / 4);
            break;
        default:
            Console.WriteLine("Неверный выбор. Выберите 1, 2 или 3!");
            break;
    }
    n--;
}
#endregion*/
